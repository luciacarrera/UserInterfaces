function validateForm(){
	var email = document.forms["login"]["email"].value;
	if(email == ""){
		alert("Please, fill the email");
		return false;
	}

	//creating the cookie (sign up)
	//loading the personal page (login)

}
